{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal permissions for the Nova Code AI shell.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
